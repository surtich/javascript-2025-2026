function add(x: number, y: number) {
    return x + y
}

const add2 = function (x: number, y: number) {
    return x + y
}

const add3 = (x: number, y:number) => {
    return x + y;
}

const add4 = (x: number, y:number) => x + y 

const inc = x => x + 1;
